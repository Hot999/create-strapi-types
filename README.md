### The two teams that will create all

Project structure
backend
frontend

In backend

```
npm i create-strapi-types
```

In the project root, execute this command

```
npx create-strapi-types
```

Parameters

```
npx create-strapi-types frontend
```

Creates a file in /frontend/src/types/types.ts