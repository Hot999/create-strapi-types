### Две команды, которые создадут все

```
Устанавливаем в backend (обязательно должна называться так)
npm i create-strapi-types
```

```
в корне проекта выполняем эту команду
npx create-strapi-types
```
